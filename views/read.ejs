<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/styles.css">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
        <div class="nav mb-10">
            <a class="text-blue-500" href="/">Home Page</a>
        </div>

        <h1 class="text-3xl tracking-tighter mb-3">All Users</h1>

        <% if (Users.length > 0) { %>
            <div class="users flex gap-3 flex-wrap">
                <% Users.forEach(function(User){ %>
                    <div class="user w-72 p-4 bg-zinc-800 rounded-lg">
                        <div class="w-full h-76 rounded-lg bg-zinc-700 overflow-hidden">
                            <img class="w-full h-52 rounded-lg bg-zinc-700 object-top" src="<%= User.image %>" alt="">
                        </div>
                        <h3 class="text-xl mt-3 tracking-tighter"> <%= User.name %> </h3>
                        <h5 class="text-zinc-500"><%= User.email %></h5>
                        <div class="flex gap-3 mt-10">
                            <a class="text-zinc-200" href="/edit/<%= User._id%>">Edit User</a>
                            <a class="text-red-500" href="/delete/<%= User._id %>">Delete User</a>
                        </div>
                    </div>
                <% }) %>
            </div>
        <% } else { %>
            <p class="text-white text-xl mt-10">No users yet</p>
        <% } %>
        
    </div>
</body>

</html>
